{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Vela 默认权限：核心 + 原生对话框（打开 / 另存为）",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
